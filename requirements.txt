fastapi
uvicorn[standard]
python-multipart
docker
groq
python-dotenv

# Database
sqlalchemy
psycopg2-binary

# Auth & Passwords
passlib[argon2]
python-jose

# AI/ML components
# Note: numpy/scipy/sklearn may be installed by sentence-transformers, but we let it manage its own deps
sentence-transformers

# Qdrant Cloud - Vector database for RAG
qdrant-client

# Utilities
httpx
websockets
